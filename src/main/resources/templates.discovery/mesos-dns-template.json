{
  "id": "mesos-dns",
  "cmd": "sudo /usr/local/mesos-dns/mesos-dns -config=/usr/local/mesos-dns/config.json",
  "cpus": 1,
  "mem": 256,
  "instances": 1,
  "constraints": [["hostname", "CLUSTER", REPLACE_IP_ADDRESS]],
  "requirePorts": true,
  "ports": [
    53,
    8123
  ]
}
